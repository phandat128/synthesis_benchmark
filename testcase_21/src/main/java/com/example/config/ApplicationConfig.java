package com.example.config;

import javax.enterprise.context.ApplicationScoped;

/**
 * Standard Quarkus configuration class.
 * Used for defining application-wide settings, producers, or global constants.
 */
@ApplicationScoped
public class ApplicationConfig {

    // This file is included as per the blueprint, but no specific configuration
    // is required for this simple service.
}